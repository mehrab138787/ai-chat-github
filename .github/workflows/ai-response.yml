name: AI Response via OpenRouter

on:
  workflow_dispatch:
    inputs:
      gist_id:
        description: 'Gist ID containing the message'
        required: true
        type: string
      user_message:
        description: 'User message'
        required: true
        type: string

jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - name: Call OpenRouter AI (Free Router)
        id: ai
        env:
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          echo "User message: ${{ inputs.user_message }}"
          
          RESPONSE=$(curl -s https://openrouter.ai/api/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $OPENROUTER_KEY" \
            -d '{
              "model": "openrouter/free",
              "messages": [{"role": "user", "content": "'"${{ inputs.user_message }}"'"}]
            }')
          
          echo "Raw response: $RESPONSE"
          
          AI_TEXT=$(echo $RESPONSE | jq -r '.choices[0].message.content // empty')
          
          if [ -z "$AI_TEXT" ]; then
            AI_TEXT="خطا: پاسخی دریافت نشد"
          fi
          
          echo "AI_TEXT=$AI_TEXT" >> $GITHUB_ENV
          echo "AI text: $AI_TEXT"
          
      - name: Update Gist with AI response
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          NEW_CONTENT=$(jq -n \
            --arg user "${{ inputs.user_message }}" \
            --arg ai "${{ env.AI_TEXT }}" \
            '{user_message: $user, ai_response: $ai, timestamp: (now | todateiso8601), status: "completed"}')
          
          curl -X PATCH \
            -H "Authorization: token $GH_TOKEN" \
            -H "Content-Type: application/json" \
            https://api.github.com/gists/${{ inputs.gist_id }} \
            -d "{\"files\":{\"message.json\":{\"content\":$(echo $NEW_CONTENT | jq -Rsa)}}}"
